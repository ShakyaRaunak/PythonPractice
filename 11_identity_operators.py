# Identity Operators

a=10
b=10
if(a is b):
	print("a, b have same identity.")
else:
	print("a, b are different.")

b=20
if(a is not b):
	print("a, b have different identity.")
else:
	print("a, b have same identity.")